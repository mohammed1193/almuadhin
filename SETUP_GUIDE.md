# دليل الإعداد والتشغيل - تطبيق المؤذن

## 📋 المتطلبات

- Python 3.8 أو أحدث
- نظام Windows 10 أو أحدث
- اتصال بالإنترنت (للمرة الأولى)

---

## 🚀 خطوات التشغيل

### 1. تثبيت المكتبات

افتح Command Prompt أو PowerShell في مجلد المشروع وقم بتشغيل:

```bash
install_dependencies.bat
```

أو يدوياً:

```bash
pip install -r requirements.txt
```

### 2. إعداد الموارد (اختياري)

#### خط تجوال:
- حمّل خط تجوال من: https://fonts.google.com/specimen/Tajawal
- ضع ملفات TTF في مجلد `resources/fonts/`

#### ملفات الأذان:
- ضع ملف صوت الأذان بصيغة MP3 في `resources/sounds/`
- سمّه `default_adhan.mp3`
- ضع ملف صوت الإقامة وسمّه `default_iqama.mp3`

**ملاحظة:** التطبيق سيعمل بدون هذه الملفات، لكن لن يشغل الأذان حتى تضيف الملفات الصوتية.

### 3. تشغيل التطبيق

```bash
run.bat
```

أو يدوياً:

```bash
cd src
python main.py
```

---

## ⚙️ الإعدادات الأولية

عند تشغيل التطبيق لأول مرة:

1. **اضغط على زر "الإعدادات"**
2. **في تبويب "عام":**
   - اضغط "تحديد تلقائي" لتحديد موقعك
   - أو أدخل المدينة والدولة يدوياً
   - اختر طريقة الحساب المناسبة

3. **في تبويب "الأذان":**
   - فعّل/عطّل الأذان لكل صلاة
   - حدد ملف الأذان إذا أضفت ملفات مخصصة
   - اضبط مستوى الصوت

4. **في تبويب "الأوقات":**
   - عدّل أوقات الصلوات بإضافة/طرح دقائق (Offset)

5. **اضغط "حفظ"**

---

## 🎯 المميزات المتاحة حالياً

✅ **عرض مواقيت الصلاة**
- جلب المواقيت من AlAdhan API
- عرض الصلوات الخمس
- عداد تنازلي للصلاة القادمة

✅ **نظام الأذان**
- تشغيل الأذان تلقائياً عند دخول الوقت
- نافذة منبثقة جميلة للأذان
- إمكانية إيقاف الأذان

✅ **الإقامة**
- تشغيل الإقامة بعد مدة محددة
- قابل للتخصيص من الإعدادات

✅ **الإشعارات**
- تنبيه قبل الصلاة بدقائق محددة
- إشعارات Windows عند دخول الوقت

✅ **System Tray**
- العمل في الخلفية
- قائمة سريعة للوصول للتطبيق

✅ **الإعدادات الشاملة**
- تخصيص كامل للأذان والأوقات
- حفظ تلقائي للإعدادات
- واجهة سهلة ومنظمة

---

## 🧪 اختبار التطبيق

لاختبار الأذان بدون انتظار:
1. افتح التطبيق
2. اضغط على زر "تجربة الأذان"
3. ستظهر نافذة الأذان مع الصوت (إذا كان متوفراً)

---

## 🐛 حل المشاكل الشائعة

### المشكلة: لا يظهر الأذان
**الحل:**
- تأكد من وجود ملف `default_adhan.mp3` في `resources/sounds/`
- تحقق من تفعيل الأذان في الإعدادات
- جرّب زر "تجربة الأذان"

### المشكلة: الأوقات غير صحيحة
**الحل:**
- تحقق من إعدادات المدينة والدولة
- جرّب طريقة حساب مختلفة
- استخدم Offset لتعديل الأوقات يدوياً

### المشكلة: لا يعمل الإنترنت
**الحل:**
- التطبيق يحتاج إنترنت للمرة الأولى فقط
- بعد ذلك يحفظ المواقيت محلياً
- اضغط "تحديث" عند توفر الإنترنت

### المشكلة: خطأ في المكتبات
**الحل:**
```bash
pip uninstall PyQt6 pygame requests
pip install -r requirements.txt
```

---

## 📝 ملاحظات مهمة

1. **الملفات الصوتية ضرورية** لتشغيل الأذان
2. **الإعدادات تُحفظ** في مجلد المستخدم: `%USERPROFILE%\.almuadhin\`
3. **التطبيق يعمل في الخلفية** عند إغلاق النافذة
4. **للخروج الكامل** استخدم "خروج" من قائمة System Tray

---

## 🔜 المميزات القادمة

- [ ] الوضع الليلي المتقدم
- [ ] التشغيل التلقائي مع Windows
- [ ] ثيمات متعددة
- [ ] دعم اللغة الإنجليزية
- [ ] إحصائيات الصلوات
- [ ] التحديثات التلقائية
- [ ] ملف تثبيت .exe

---

## 💡 نصائح الاستخدام

- استخدم **Ctrl + Q** للخروج السريع (قريباً)
- اضغط مرتين على أيقونة System Tray لإظهار النافذة
- يمكنك تخصيص صوت مختلف لكل صلاة (قريباً)
- التطبيق يحدّث المواقيت تلقائياً كل يوم

---

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل:
- راجع ملف `DEVELOPER_GUIDE.md` للتفاصيل التقنية
- تحقق من `README.md` للمعلومات العامة
