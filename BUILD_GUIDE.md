# دليل بناء ملف EXE للمؤذن

## 📦 بناء ملف تنفيذي (.exe)

هذا الدليل يشرح كيفية بناء ملف تنفيذي للتطبيق يعمل بشكل مستقل بدون الحاجة لـ Python.

---

## ✅ المتطلبات

1. **Python مثبت** على جهازك
2. **جميع المكتبات المطلوبة** مثبتة (من requirements.txt)
3. **PyInstaller** - سيتم تثبيته تلقائياً

---

## 🚀 الطريقة السريعة (موصى بها)

### استخدام ملف BAT

ببساطة قم بتشغيل:

```batch
build_simple.bat
```

هذا الملف سيقوم بكل شيء تلقائياً:
- تثبيت PyInstaller إذا لم يكن مثبتاً
- بناء ملف EXE
- وضعه في مجلد `dist`

---

## 🔧 الطريقة المتقدمة

### استخدام Python Script

```bash
python build_exe.py
```

هذا السكريبت يوفر تحكم أكبر وتقارير مفصلة.

---

## 📁 النتيجة

بعد البناء، ستجد:

```
dist/
└── المؤذن.exe    ← الملف التنفيذي الجاهز
```

### معلومات الملف:
- **الاسم:** المؤذن.exe
- **الحجم:** ~50-80 MB تقريباً
- **الأيقونة:** أيقونة إسلامية مخصصة (هلال ونجمة)
- **النوع:** ملف واحد مستقل (onefile)

---

## ✨ المميزات

### ✅ **اسم التطبيق يظهر في كل مكان:**
- شريط العنوان: "المؤذن - مواقيت الصلاة"
- شريط المهام: "المؤذن"
- Alt+Tab: "المؤذن"
- مدير المهام: "المؤذن.exe"
- خصائص الملف: "المؤذن"

### ✅ **أيقونة مخصصة:**
- لا يظهر رمز exe الافتراضي
- أيقونة إسلامية جميلة (هلال ونجمة أخضر)
- تظهر في:
  - أيقونة الملف
  - شريط المهام
  - Alt+Tab
  - System Tray

### ✅ **معلومات الملف:**
- اسم الشركة: محمد الدخيل
- وصف الملف: المؤذن - تطبيق مواقيت الصلاة
- الإصدار: 1.0.0.0
- حقوق النشر: © 2025 محمد الدخيل

---

## 🎯 كيفية الاستخدام بعد البناء

### 1. **تشغيل مباشر**
- افتح مجلد `dist`
- اضغط مرتين على `المؤذن.exe`

### 2. **نسخ إلى مكان آخر**
- يمكنك نسخ الملف إلى أي مكان
- يعمل بشكل مستقل تماماً
- لا يحتاج Python

### 3. **إنشاء اختصار**
- انقر بالزر الأيمن على الملف
- اختر "إنشاء اختصار"
- ضع الاختصار على سطح المكتب

### 4. **التشغيل التلقائي**
- افتح التطبيق
- اذهب للإعدادات
- فعّل "تشغيل تلقائي عند بدء Windows"

---

## 🔍 حل المشاكل

### **المشكلة: البناء يفشل**
**الحل:**
```bash
# تأكد من تثبيت PyInstaller
pip install pyinstaller

# تأكد من تثبيت جميع المكتبات
pip install -r requirements.txt

# حاول مرة أخرى
build_simple.bat
```

### **المشكلة: الملف كبير جداً**
**الحل:**
- هذا طبيعي، الملف يحتوي على Python كامل
- الحجم النموذجي: 50-80 MB
- يمكن ضغطه بـ ZIP إلى ~30 MB

### **المشكلة: الأيقونة لا تظهر**
**الحل:**
```bash
# تأكد من وجود ملف الأيقونة
dir resources\icons\app_icon.ico

# إذا لم يكن موجوداً، أنشئه
python create_app_icon.py
```

### **المشكلة: الملف لا يعمل على جهاز آخر**
**الحل:**
- تأكد من نسخ مجلد `resources` بجانب الملف
- أو استخدم `--onefile` في البناء (مستخدم بالفعل)

---

## 📝 ملاحظات مهمة

### ✅ **ما يتم تضمينه في الملف:**
- Python كامل
- جميع المكتبات المطلوبة
- مجلد resources (الأصوات، الأيقونات، الخطوط)
- جميع ملفات الكود

### ⚠️ **ما لا يتم تضمينه:**
- ملفات الإعدادات (تُنشأ تلقائياً عند التشغيل)
- ملفات الأذان المخصصة (يجب إضافتها يدوياً)

### 💡 **نصائح:**
1. **اختبر الملف قبل التوزيع**
   - جرّبه على جهازك
   - جرّبه على جهاز آخر

2. **احتفظ بنسخة احتياطية**
   - احفظ الملف في مكان آمن
   - احفظ الكود المصدري

3. **التحديثات**
   - عند تعديل الكود، أعد البناء
   - غيّر رقم الإصدار في `version_info.txt`

---

## 🎨 تخصيص الأيقونة

إذا أردت تغيير الأيقونة:

1. **ضع ملف ICO جديد** في:
   ```
   resources/icons/app_icon.ico
   ```

2. **أو أنشئ أيقونة جديدة:**
   ```bash
   python create_app_icon.py
   ```

3. **أعد البناء:**
   ```bash
   build_simple.bat
   ```

---

## 📊 معلومات البناء

### **الإعدادات المستخدمة:**
- `--onefile`: ملف واحد مستقل
- `--windowed`: بدون نافذة console
- `--icon`: أيقونة مخصصة
- `--add-data`: تضمين مجلد resources
- `--clean`: تنظيف الملفات المؤقتة

### **المكتبات المخفية:**
يتم تضمين هذه المكتبات تلقائياً:
- PyQt6
- pygame
- requests
- geocoder
- pytz
- جميع modules التطبيق

---

## 🚀 التوزيع

### **للاستخدام الشخصي:**
- شغّل الملف مباشرة

### **للتوزيع:**
1. اضغط الملف بـ ZIP
2. أضف ملف README
3. أضف ملفات أذان نموذجية
4. شارك الملف

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع قسم "حل المشاكل" أعلاه
2. تأكد من تثبيت جميع المكتبات
3. جرّب البناء مرة أخرى

---

**استمتع بتطبيق المؤذن! 🕌**
