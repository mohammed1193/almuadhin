# دليل المطور - تطبيق المؤذن

## 🚀 البدء السريع

### 1. تثبيت المكتبات المطلوبة

```bash
pip install -r requirements.txt
```

### 2. تشغيل التطبيق

```bash
cd src
python main.py
```

## 📁 هيكل المشروع

```
المؤذن/
├── src/
│   ├── main.py                 # نقطة البداية
│   ├── ui/
│   │   ├── __init__.py
│   │   └── main_window.py      # النافذة الرئيسية
│   ├── core/
│   │   ├── __init__.py
│   │   ├── api_client.py       # التواصل مع AlAdhan API
│   │   └── prayer_times.py     # إدارة مواقيت الصلاة
│   └── utils/
│       ├── __init__.py
│       ├── config.py           # إدارة الإعدادات
│       └── location.py         # تحديد الموقع
├── resources/
│   ├── fonts/                  # خط تجوال
│   ├── sounds/                 # ملفات الأذان والإقامة
│   ├── icons/                  # الأيقونات
│   └── themes/                 # ملفات الثيمات
├── tests/                      # الاختبارات
├── requirements.txt            # المكتبات المطلوبة
├── .gitignore
└── README.md
```

## 🔧 المكونات الرئيسية

### 1. API Client (`core/api_client.py`)
- الاتصال بـ AlAdhan API
- جلب مواقيت الصلاة
- معالجة الأخطاء

### 2. Prayer Times Manager (`core/prayer_times.py`)
- إدارة مواقيت الصلاة
- تطبيق Offset
- حساب الوقت المتبقي

### 3. Config Manager (`utils/config.py`)
- حفظ/قراءة الإعدادات
- الإعدادات الافتراضية
- التحقق من صحة البيانات

### 4. Main Window (`ui/main_window.py`)
- الواجهة الرئيسية
- عرض المواقيت
- العداد التنازلي

## 📝 المراحل القادمة

- [ ] نظام الجدولة والأذان
- [ ] مشغل الصوت
- [ ] نافذة الإعدادات
- [ ] System Tray
- [ ] الوضع الليلي
- [ ] التشغيل التلقائي
- [ ] التثبيت والتوزيع

## 🧪 الاختبار

```bash
cd tests
python -m pytest
```

## 📦 بناء التطبيق

```bash
pyinstaller --name="المؤذن" --windowed --onefile src/main.py
```
