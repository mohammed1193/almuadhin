# دليل الرفع اليدوي على GitHub 📤

## الطريقة الأسهل: الرفع من المتصفح مباشرة

لا تحتاج Git أو أوامر معقدة! فقط المتصفح.

---

## 📝 الخطوات بالتفصيل

### الخطوة 1: اذهب إلى Repository
افتح المتصفح واذهب إلى:
```
https://github.com/mohammed1193/almuadhin
```

---

### الخطوة 2: اضغط "Add file"
في الصفحة الرئيسية للـ repository:
1. ابحث عن زر **"Add file"** (في الأعلى)
2. اضغط عليه
3. اختر **"Upload files"**

---

### الخطوة 3: اختر الملفات

#### الطريقة الأولى: السحب والإفلات (Drag & Drop)
1. افتح مجلد المشروع: `d:\تطبيق المؤذن`
2. اسحب الملفات والمجلدات من النافذة
3. أفلتها في صفحة GitHub (حيث مكتوب "Drag files here")

#### الطريقة الثانية: اختيار الملفات
1. اضغط **"choose your files"**
2. اختر الملفات من مجلد المشروع
3. يمكنك اختيار عدة ملفات مرة واحدة (Ctrl + Click)

---

### الخطوة 4: ما الذي يجب رفعه؟

#### ✅ ارفع هذه الملفات والمجلدات:

**المجلدات:**
- ✅ `src/` (كل المجلد)
- ✅ `resources/` (كل المجلد)

**الملفات المهمة:**
- ✅ `README.md`
- ✅ `PRIVACY_POLICY.md`
- ✅ `PRIVACY_POLICY_EN.md`
- ✅ `STORE_LISTING.md`
- ✅ `MICROSOFT_STORE_GUIDE.md`
- ✅ `STORE_READINESS_CHECKLIST.md`
- ✅ `QUICK_START_STORE.md`
- ✅ `requirements.txt`
- ✅ `version_info.txt`
- ✅ `build_exe.py`
- ✅ `build_simple.bat`
- ✅ `run.bat`
- ✅ جميع ملفات `.md` الأخرى

#### ❌ لا ترفع هذه:
- ❌ `dist/` (ملفات مبنية)
- ❌ `build/` (ملفات مؤقتة)
- ❌ `__pycache__/` (ملفات Python المؤقتة)
- ❌ `.git/` (إذا موجود)
- ❌ `*.pyc` (ملفات Python المترجمة)
- ❌ `المؤذن.spec` (ملف PyInstaller)

---

### الخطوة 5: اكتب رسالة Commit

في الأسفل، ستجد مربعين:
1. **العنوان:** اكتب `Initial commit - Al-Muadhin Prayer Times App`
2. **الوصف (اختياري):** يمكنك تركه فارغاً أو كتابة وصف

---

### الخطوة 6: اضغط "Commit changes"

اضغط الزر الأخضر **"Commit changes"** في الأسفل.

---

### الخطوة 7: انتظر قليلاً

GitHub سيرفع الملفات (قد يأخذ 1-3 دقائق حسب حجم الملفات).

---

## ✅ التحقق من النجاح

بعد الرفع:
1. حدّث الصفحة (F5)
2. يجب أن ترى جميع الملفات والمجلدات
3. جرب فتح: `PRIVACY_POLICY.md` للتأكد

---

## 🌐 الحصول على رابط سياسة الخصوصية

بعد رفع الملفات، الرابط سيكون:
```
https://github.com/mohammed1193/almuadhin/blob/main/PRIVACY_POLICY.md
```

**هذا الرابط يعمل فوراً!** استخدمه في متجر مايكروسفت.

---

## 🔄 إذا أردت تحديث ملف

1. اذهب إلى الملف على GitHub
2. اضغط أيقونة القلم (✏️) "Edit"
3. عدّل الملف
4. اضغط "Commit changes"

---

## 📁 رفع مجلد كامل

**ملاحظة مهمة:** GitHub لا يسمح برفع مجلدات فارغة.

لرفع مجلد:
1. اضغط "Add file" > "Create new file"
2. اكتب اسم المجلد متبوعاً بـ `/` ثم اسم ملف
   مثال: `src/main.py`
3. الصق محتوى الملف
4. Commit

**أو الأسهل:** ارفع الملفات داخل المجلد مباشرة، وGitHub سينشئ المجلد تلقائياً.

---

## 💡 نصائح مهمة

### 1. رفع دفعات صغيرة
لا ترفع كل شيء مرة واحدة. ارفع:
- أولاً: المجلدات الأساسية (`src/`, `resources/`)
- ثانياً: ملفات `.md`
- ثالثاً: باقي الملفات

### 2. تحقق من الحجم
GitHub يسمح بملفات حتى 100 MB للملف الواحد.
إذا كان ملف الصوت كبير جداً، قد تحتاج ضغطه.

### 3. استخدم .gitignore (اختياري)
يمكنك إنشاء ملف `.gitignore` لتجنب رفع ملفات غير مرغوبة:

```
# محتوى .gitignore
dist/
build/
__pycache__/
*.pyc
*.pyo
*.spec
.git/
```

---

## 🎯 الخطوات السريعة (ملخص)

1. ✅ اذهب إلى: https://github.com/mohammed1193/almuadhin
2. ✅ اضغط "Add file" > "Upload files"
3. ✅ اسحب المجلدات: `src/` و `resources/`
4. ✅ اسحب الملفات: جميع `.md` و `requirements.txt`
5. ✅ اكتب commit message: "Initial commit"
6. ✅ اضغط "Commit changes"
7. ✅ انتظر الرفع
8. ✅ تحقق من الملفات

---

## 🔗 بعد الرفع

استخدم هذا الرابط في متجر مايكروسفت:
```
https://github.com/mohammed1193/almuadhin/blob/main/PRIVACY_POLICY.md
```

---

## ❓ مشاكل شائعة

### المشكلة: "File is too large"
**الحل:** الملف أكبر من 100 MB. ضغطه أو استخدم Git LFS.

### المشكلة: لا أستطيع رفع مجلد
**الحل:** ارفع الملفات داخل المجلد، وسينشأ المجلد تلقائياً.

### المشكلة: الملفات العربية لا تظهر بشكل صحيح
**الحل:** هذا طبيعي في بعض الملفات. المهم أن الملفات موجودة.

---

**الآن جرب! الطريقة سهلة جداً ولا تحتاج أي برامج إضافية.** 🚀
